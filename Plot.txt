%notes for possible (manual) plot
%vector to plot 2d (not necessary)
polmat = policy(:,1);
polmat = transpose(polmat);
%book p.229 (254 in pdf search)
%consumption should be given in the code though (where?)
cons = zvec.*(kvec.^alpha) + (1-delta).*kvec-polmat;
%x-axis capital stock, y-axis consumtpion level
%-> the kink does not really make sense there 
plot(policy,cons)

%alternative plot (with code values) 
%??
surf(cons,zvec,policy)

%TFP = output growth - alpha*(capital growth) [-((1-alpha)*labor growth)]